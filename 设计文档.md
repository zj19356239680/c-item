# ApiGate 设计文档

本文描述当前 0.3.0 代码的架构与取舍，不是功能承诺。首次安装、运行、配置默认值、
HTTP 示例和 Docker 命令见 [README](README.md)。本项目在 Linux 环境下开发，
仅支持 Linux 构建与运行；使用 C++20，CMake 在非 Linux 平台明确失败。

## 目标、现状与边界

项目的长期目标是逐步形成带路由、上游转发、进程内缓存和限流的轻量 API 网关。
目前**已实现**的只有环境配置校验、单行 JSON 日志、基于 Boost.Asio/Beast 的
HTTP/1.1 服务、`GET /healthz`、`GET /readyz`、受限请求解析和信号退出。
`/readyz` 只反映当前进程能响应请求，不验证尚不存在的上游或缓存。

**尚未实现**反向代理、可配置路由、上游重试、缓存、限流、认证、指标、管理页面、
TLS 和 HTTP/2。因此不能将当前服务暴露为生产 API 网关。本文记录的是现有行为，
不是对规划能力或长期兼容性的承诺。

## 架构与模块职责

```text
环境变量 / 命令行
        │
        ▼
main → config → StructuredLogger → Application / io_context
                                      ├─ signal_set (SIGINT/SIGTERM)
                                      └─ HttpServer / acceptor
                                              └─ HttpSession / Beast parser
                                                      └─ HTTP handler → JSON 响应
```

| 模块 | 当前职责 | 边界 |
| --- | --- | --- |
| `src/main.cpp` | 命令行分派、装配对象、最外层异常处理 | 不处理网络请求 |
| `src/config.cpp` | 读取固定的五项环境变量、应用默认值和校验 | 不打开监听器 |
| `src/logging.cpp` | 为正常事件输出 UTC 时间戳和 JSON payload | 不记录请求头或正文 |
| `src/application.cpp` | 拥有 `io_context`、信号等待和服务生命周期 | 不决定 HTTP 路由 |
| `src/http_server.cpp` | 监听、管理会话、异步读写、限制与停机 | 不实现上游代理 |
| `src/http_handler.cpp` | 将方法和路径映射成 JSON 响应 | 不拥有 socket |

公开头文件在 `include/apigate/`；`tests/unit/` 和 `tests/integration/` 分别验证
处理逻辑与真实进程行为。`CMakeLists.txt` 定义 `apigate_core`、可执行程序、测试和
安装目标；`CMakePresets.json` 定义 Debug、Release 和分析预设；`deploy/` 放置
Dockerfile 与 Compose；`scripts/` 负责安装、构建、检查、运行和容器烟雾测试。

### 单线程事件循环

`Application::run()` 在一个线程调用 `io_context.run()`。监听、会话读写和信号
回调由同一事件循环调度：等待网络 I/O 时不用为每条连接占用一个线程，共享的会话
集合目前也不需要跨线程锁。这是当前简单性和可验证性的选择，不是并发性能保证。

代价是一个阻塞或长时间运行的回调（包括同步日志写入）会拖慢所有连接。未来增加
上游调用或多线程前，必须先明确对象所有权、串行化和背压策略，不应直接把
`io_context.run()` 放到多线程中。

## 配置与启动

`main` 先解析命令行，再加载配置、创建日志器和运行 `Application`。配置校验
发生在监听前：标签限制为 1–64 个允许字符，日志等级来自固定集合，监听地址必须
是 IPv4/IPv6 字面地址，端口必须是 0–65535 的整数。端口 0 由内核分配，实际端口
写入 `http_listener_started` 日志。具体默认值见 [README 配置表](README.md#配置)。

`--check-config` 会加载配置，并在配置合法时向标准输出写入恰好一条
`configuration_valid` 单行 JSON 后返回 0，但不创建监听 socket；它不能检测端口
冲突。该成功结果是命令输出而非可选服务日志，因此不受 `APIGATE_LOG_LEVEL` 阈值
影响。配置项数量虽固定，字符串仍要复制、扫描和解析，所需时间与输入长度相关，
不能将配置解析笼统断言为 O(1)。

正常启动先绑定并监听，再注册异步 accept 与信号等待，随后执行事件循环。
绑定失败时返回非零状态；`http_server_start_failed` 由已经建立的结构化日志器
记录。`scripts/run.sh` 在执行程序前还会按所选预设构建，脚本的构建耗时不属于
程序启动耗时。

## HTTP 请求与资源边界

`HttpServer` 接受 TCP 连接并创建 `HttpSession`。会话使用 Beast parser 读入完整
请求，`handle_http_request` 按方法和去掉查询字符串后的路径生成响应，再异步
写出；keep-alive 连接继续读下一条请求。JSON 正文由 `nlohmann/json` 构造，
该库也用于日志 payload。

| 规则 | 当前值或行为 | 目的与代价 |
| --- | --- | --- |
| 请求头上限 | 8 KiB；超限通常返回 431 | 限制单请求解析空间；不适合大头部请求 |
| 请求体上限 | 64 KiB；超限通常返回 413 | 防止无限制读入；`GET` 正文即使合法也不会被业务使用 |
| 读、写期限 | 各次异步读或写前设为 10 秒 | 限制慢连接；不是整条连接或进程的总期限 |
| keep-alive | 遵循请求，单连接最多处理 100 次 | 避免无限复用；不限制总连接数 |
| 非法请求 | 400 或安全关闭连接 | socket 已不可写时不保证一定返回错误正文 |

HTTP handler 对 `GET /healthz` 和 `GET /readyz` 返回 200；其他 `GET` 路径
返回 404；所有非 `GET` 方法先返回 405 并设置 `Allow: GET`。正常生成的响应
设置 `Server: ApiGate`、`Content-Type: application/json`、
`Cache-Control: no-store`，并由 Beast 准备正文长度和连接语义。
具体 JSON 正文见 [README HTTP 行为表](README.md#当前-http-行为)。

大小限制约束的是**单个请求/会话**，而不是进程总内存；会话还持有解析器、缓冲区
和响应对象。当前没有全局连接数限制，这些设置不足以构成完整的抗滥用方案。
路径匹配为固定的两个端点；不存在路由配置、上游访问或缓存失效逻辑。

## 停止、异常与日志

收到 `SIGINT` 或 `SIGTERM` 后，信号回调记录事件，关闭 acceptor，取消重试计时器，
并取消、关闭所有活动 socket。异步完成回调随后释放会话，`io_context.run()`
返回后记录 `service_stopped`。这能及时释放资源，但**不会等待在途响应排空**；
客户端可能看到连接中断。

正常 `StructuredLogger` 日志为单行 JSON：顶层有 `timestamp`、`level`、
`payload`；payload 包含 `service`、`environment`、`event` 和事件字段。
请求日志只记录方法、分类路由、状态码和远端地址，不记录原始 URL、查询参数、
请求头或正文。监听启动失败发生在日志器建立后，遵循这一结构；命令行/配置等
更早的启动失败由 `main` 写出简化的 `bootstrap_failed` JSON，其 `event`、
`category` 和 `message` 位于顶层，没有上述时间戳或 payload。极端清理失败
的后备诊断也可能缺少正常日志字段，不能把三者当作同一日志模式解析。

配置错误、命令行错误和运行时异常最终导致非零退出；异步 I/O 错误经
`error_code` 分支处理。非法配置的错误消息不回显其具体值。当前没有指标、
分布式追踪或请求 ID；健康端点和日志不能替代完整的可观测性系统。

## 构建与部署决策

| 选择 | 原因与当前用法 |
| --- | --- |
| Boost.Asio + Beast | 同一事件循环承载 TCP、信号及 HTTP/1.1 解析/序列化 |
| nlohmann/json + spdlog | 构造 HTTP/日志 JSON；由 spdlog 输出正常事件 |
| CMake + Ninja + vcpkg | Linux 预设统一参数；`vcpkg.json` 锁定依赖基线，GoogleTest 仅通过测试 feature 引入 |
| clang-format、clang-tidy、ASan/UBSan | 格式、静态分析及运行期错误检查 |

`linux-debug` 和 `linux-release` 用于日常构建与测试；`linux-analysis` 启用
警告即错误、clang-tidy、ASan/UBSan。系统包需由普通用户交互执行 `sudo` 命令安装；
`scripts/bootstrap.sh` 检查系统包、准备 vcpkg 依赖并运行完整检查。
下载使用有界重试和本地缓存，但全新环境仍需网络。
具体命令及缓存路径见 [README](README.md#构建测试与运行)。

Dockerfile 在构建阶段运行 Release 测试，运行阶段仅复制安装后的程序，以非 root
用户启动，并定义 `/healthz` 健康检查与 `SIGTERM` 停止信号。只读根文件系统、
`cap_drop: ALL`、`no-new-privileges` 和宿主机回环端口映射由 Compose 配置；
单独 `docker run` 时必须显式提供相应限制。容器部署不增加代理、TLS 或认证能力。

## 验证范围

| 现有测试或检查 | 验证内容 |
| --- | --- |
| `tests/unit/config_test.cpp` | 默认值、合法覆盖、标签边界、空值和非法等级/地址/端口 |
| `tests/unit/http_handler_test.cpp` | 健康端点、查询字符串、404、405/`Allow`、keep-alive 偏好 |
| `tests/unit/logging_test.cpp` | 正常结构化日志的等级阈值过滤 |
| `tests/integration/service_lifecycle.sh` | 动态端口启动；`SIGTERM` 与 `SIGINT` 后正常退出及事件日志 |
| `tests/integration/http_server_test.py` | 真实 HTTP 响应、连接复用、413/431、格式错误、端口占用和停机；正常日志 schema；查询参数、认证头、Cookie 和正文不落日志；`--check-config` 在全部日志等级下的稳定 JSON、非法配置及不监听 |
| CTest `integration.config_check` | 配置检查命令可成功退出 |
| `scripts/check.sh` | 格式检查、ShellCheck、Python 语法、clang-tidy、警告即错误及 ASan/UBSan 测试 |
| `scripts/container-smoke.sh` | 镜像构建、容器健康与 HTTP、非 root/只读/移除 capabilities、`SIGTERM` 退出 |

进程测试使用动态端口和超时，退出时清理自己启动的进程。它们尚未覆盖并发容量、
在途请求排空或上游代理；测试通过也不构成对外兼容承诺。

## 当前决策与重新评估条件

### API 兼容政策

**当前决策（0.3.0）：**不承诺版本化或长期向后兼容，也不把内部 C++ 接口视为公开
API。当前可见的 HTTP 行为只有健康端点、404/405 和受限请求的错误处理；程序还暴露
五项 `APIGATE_*` 环境变量及 `--check-config`。Docker 健康检查依赖
`/healthz`；`/readyz`、响应和配置行为受现有测试约束，但测试覆盖不等于兼容
承诺。对这些现有行为的改变应同步更新文档、测试和发布说明，避免无提示破坏。
代理路由和上游 API 尚不存在，不能预先承诺其格式。Compose 的
`APIGATE_HOST_PORT` 仅控制宿主机端口映射，不是程序配置项。

**重新评估条件：**在首个外部消费者或真实代理路由投入使用之前，明确哪些路径、
方法、状态码、JSON 字段及含义、环境变量和命令行行为属于受支持契约，并制定
版本、弃用与迁移规则。改动端点、探针语义、状态码、字段类型/含义或配置默认值
可能破坏使用者；新增路由也可能改变原先的 404。届时先说明新旧行为及迁移方法，
必要时提供过渡期，不凭空承诺固定期限或把 JSON 字段顺序当作契约。

### 连接准入与有截止时间的停机排空

**当前决策：**现有 8 KiB 请求头、64 KiB 正文、单次异步读/写 10 秒和每连接最多
100 个请求，仅限制单连接行为。`HttpSession` 还持有 socket、最多 72 KiB 的
`flat_buffer` 容量、解析器、正文和响应；活动会话虽由集合管理，却没有进程级
连接数上限。收到 `SIGTERM` 后立即停止接收并取消、关闭所有活动连接，**没有**
等待在途请求完成。`/readyz` 只要能被请求就返回 200；Docker 健康检查使用
`/healthz`，不是 `/readyz`。

**重新评估条件与真实代理流量前要求：**先确定目标负载及资源预算，测量空闲和
在途连接的内存、文件描述符、CPU/事件循环延迟、请求延迟与错误率，以及未来
上游容量；据此设置可验证的连接准入上限和拒绝策略，不预填容量数字。停机应使用
单调时钟截止时间：标记不再就绪、停止接新连接、立即关闭空闲 keep-alive 和
未完成请求、让已分派的在途请求完成并关闭连接，到期强制取消剩余操作并释放资源。
`/readyz` 应只在仍可接收工作时表示就绪；关闭监听器后，新探针可能得到连接失败
而不是 503。若部署系统必须先观察到 503 再摘流，需要独立的预排空阶段，不能
仅依赖当前 SIGTERM 流程。

Compose 的 `stop_grace_period: 10s` 是强制终止前的总宽限，不是当前排空时长。
将来选定的应用排空期限加上退出清理余量必须短于该宽限；若测量表明不足，应先
调整部署配置。新增测试至少覆盖达到/超过准入上限及槽位回收、空闲连接关闭、
在途请求期限内完成、到期强制关闭、就绪状态转换，以及进程在容器强制终止前
退出；这些测试与负载数据共同决定验收，不能用现有短请求测试代替。
