# ApiGate 设计文档

## 1. 项目概述

**ApiGate** 是一个以 C++ 为核心实现的轻量级 HTTP API 网关。

它位于客户端与后端服务之间，负责请求转发、轮询负载均衡、本地缓存、请求限流、健康检查和基础监控。项目目标是实践 C++ 后端开发中的网络编程、并发控制、缓存设计、工程化与可观测性。

```text
Client / Web Console
        │
        ▼
   ApiGate (C++)
   ├─ HTTP 接入与路由
   ├─ 负载均衡
   ├─ LRU + TTL 缓存
   ├─ Token Bucket 限流
   ├─ 日志与指标
   └─ 健康检查
        │
        ├────────► Backend A
        └────────► Backend B
```

## 2. 项目目标

- 使用 C++20 实现 HTTP/1.1 API 网关。
- 将客户端请求转发到多个固定后端服务。
- 对后端服务实现 Round Robin 轮询负载均衡。
- 对部分 GET 请求提供本地 LRU + TTL 缓存。
- 按客户端 IP 实现 Token Bucket 限流。
- 记录 QPS、延迟、错误数、缓存命中率和限流次数。
- 提供简单 Web 管理页展示指标与后端状态。
- 通过单元测试、集成测试、压测和 Docker Compose 完成工程交付。

## 3. 非目标

第一版不实现以下功能：

- HTTPS、HTTP/2、WebSocket；
- 自研 HTTP 协议解析器；
- 服务注册发现、动态配置中心；
- 分布式缓存、Redis 集群；
- 一致性哈希、主从复制、Kubernetes；
- 无锁队列、协程运行时；
- 完整认证鉴权体系。

这些能力可作为后续扩展，但不能影响 MVP 交付。

## 4. 核心功能

### 4.1 HTTP 请求转发

- 接收 HTTP/1.1 请求。
- 根据请求路径匹配路由规则。
- 将请求转发至指定后端服务。
- 返回后端响应给客户端。
- 支持常见方法：`GET`、`POST`、`PUT`、`DELETE`。
- 转发必要请求头，并补充 `X-Forwarded-For`。

错误码约定：

| 场景 | 响应码 |
| --- | ---: |
| 请求格式不合法 | 400 |
| 无匹配路由 | 404 |
| 触发限流 | 429 |
| 后端服务不可用 | 502 |
| 后端响应超时 | 504 |
| 网关内部异常 | 500 |

### 4.2 负载均衡

- 每个路由配置多个后端地址。
- 第一版采用 Round Robin。
- 通过原子计数器选择后端，避免全局锁。
- 后端失败时记录失败次数，并在健康检查中更新状态。

### 4.3 本地缓存

仅缓存显式配置为可缓存的 GET 请求。

缓存规则：

- Cache Key：`HTTP Method + Path + Query String`。
- 仅缓存状态码为 `200` 的响应。
- 缓存最大响应体大小可配置。
- 缓存容量满时按 LRU 淘汰。
- 每项缓存配置 TTL，过期后失效。
- 不缓存带认证信息或包含敏感响应头的请求。

数据结构：

- `std::unordered_map`：O(1) 查找缓存节点。
- `std::list`：维护最近访问顺序。
- `std::priority_queue`：管理 TTL 到期事件。
- `std::shared_mutex`：缓存读多写少时提供并发保护。

### 4.4 限流

- 默认按客户端 IP 限流。
- 第一版采用 Token Bucket 算法。
- 每条路由可配置容量和令牌补充速率。
- 超出额度时返回 `429 Too Many Requests`。
- 限流记录自动过期，避免 IP 状态无限增长。

### 4.5 健康检查

- 每个后端服务提供 `/health` 接口。
- ApiGate 定期探测后端状态。
- 标记不健康的后端不参与负载均衡。
- 所有后端不可用时返回 `502`。

### 4.6 指标与日志

提供 `/metrics` 接口，至少包含：

- 总请求数；
- 当前 QPS；
- 2xx、4xx、5xx 数量；
- 缓存命中数和命中率；
- 限流次数；
- 后端请求失败次数；
- 请求平均延迟与 p95 延迟；
- 后端健康状态。

日志应包含：

- 请求 ID；
- 请求方法与路径；
- 客户端 IP；
- 选中的后端；
- 响应状态码；
- 请求耗时；
- 缓存命中与限流结果。

## 5. 技术栈

| 类别 | 技术选择 | 用途 |
| --- | --- | --- |
| 语言标准 | C++20 | 项目核心实现 |
| 网络与 HTTP | Boost.Asio、Boost.Beast | 异步 I/O、HTTP 接入与转发 |
| 构建系统 | CMake | 跨平台构建 |
| 依赖管理 | vcpkg | 管理第三方库 |
| 配置解析 | nlohmann/json | 解析 JSON 配置 |
| 日志 | spdlog | 结构化日志输出 |
| 单元测试 | GoogleTest | 模块级测试 |
| 前端 | HTML、CSS、JavaScript | 管理控制台 |
| 压测 | `hey` 或 `wrk` | 测试吞吐量与延迟 |
| 容器化 | Docker Compose | 启动网关和模拟后端 |
| 可选持久化 | MySQL | 保存路由配置与审计日志 |
| 可选扩展 | Redis | 分布式限流或远程缓存实现 |

## 6. 项目结构

```text
api-gate/
├─ CMakeLists.txt
├─ README.md
├─ config/
│  └─ gateway.json
├─ docs/
│  ├─ DESIGN.md
│  └─ API.md
├─ include/
│  ├─ gateway/
│  ├─ cache/
│  ├─ limiter/
│  ├─ load_balancer/
│  ├─ health/
│  └─ metrics/
├─ src/
│  ├─ main.cpp
│  ├─ gateway/
│  ├─ cache/
│  ├─ limiter/
│  ├─ load_balancer/
│  ├─ health/
│  ├─ metrics/
│  └─ config/
├─ tests/
│  ├─ cache_test.cpp
│  ├─ limiter_test.cpp
│  ├─ load_balancer_test.cpp
│  └─ integration_test.cpp
├─ web/
│  ├─ index.html
│  ├─ app.js
│  └─ styles.css
└─ docker/
   └─ docker-compose.yml
```

## 7. 配置示例

```json
{
  "server": {
    "host": "0.0.0.0",
    "port": 8080,
    "request_timeout_ms": 3000
  },
  "routes": [
    {
      "prefix": "/api/users",
      "backends": [
        "http://backend-a:9001",
        "http://backend-b:9002"
      ],
      "cache": {
        "enabled": true,
        "ttl_seconds": 30,
        "max_entries": 1000
      },
      "rate_limit": {
        "enabled": true,
        "capacity": 100,
        "refill_per_second": 20
      }
    }
  ]
}
```

## 8. 开发阶段

### 阶段 1：工程与最小转发

- 初始化 CMake、vcpkg 和基础目录。
- 启动 HTTP 服务。
- 转发请求到一个固定后端。
- 使用 `curl` 验证请求链路。

验收：客户端请求可经 ApiGate 返回后端响应。

### 阶段 2：路由与负载均衡

- 解析 JSON 路由配置。
- 支持两个固定后端。
- 实现 Round Robin。
- 增加超时和错误响应。

验收：连续请求能轮流命中两个后端。

### 阶段 3：缓存与限流

- 实现线程安全 LRU Cache。
- 实现 TTL 过期管理。
- 实现 Token Bucket 限流。
- 为缓存和限流添加 GoogleTest。

验收：缓存命中时不访问后端；限流超额时返回 429。

### 阶段 4：健康检查与可观测性

- 实现后端健康检查。
- 实现 `/metrics`。
- 接入 `spdlog`。
- 增加请求 ID 和延迟统计。

验收：后端下线后不再被选中；指标可反映请求状态。

### 阶段 5：前端与交付

- 实现轻量管理页面。
- 编写 API 文档和架构说明。
- 编写 Docker Compose。
- 进行压测并记录结果。
- 录制演示视频。

验收：新环境可通过一条命令启动并完成演示。

## 9. 测试要求

- LRU 的读写、淘汰、更新、容量边界测试。
- TTL 的到期、重复更新、旧堆节点测试。
- Token Bucket 的令牌补充和并发访问测试。
- Round Robin 的轮询正确性测试。
- 后端超时、后端宕机、全部后端不可用测试。
- 缓存命中与未命中的集成测试。
- 限流触发后的 HTTP 状态码测试。

## 10. 性能与交付要求

性能结果必须注明测试环境，不得编造数据。压测报告至少包含：

- CPU、内存、操作系统；
- 后端数量；
- 并发连接数；
- 请求总量；
- QPS；
- p50、p95、p99 延迟；
- 错误率；
- 开启和关闭缓存时的对比。

最终仓库应包含：

- 可运行源码；
- 清晰 README；
- 架构图；
- API 文档；
- 单元测试；
- Docker Compose；
- 压测报告；
- 演示视频链接。

## 11. Agent 开发约束

- 一次只实现一个模块，不得一次性生成完整系统。
- 每个模块必须先定义职责、接口、测试用例和验收标准。
- 修改缓存、限流、并发代码后必须运行对应测试。
- 使用成熟库处理 HTTP 协议与异步 I/O，不手写 HTTP 解析器。
- 不为“看起来高级”而添加未验证的分布式、无锁或集群功能。
- 每完成一个可验证功能，创建一次语义明确的 Git commit。
