# ApiGate Coding Agent 规则

本文件适用于整个仓库。`README.md` 说明当前可用行为和使用方式，`设计文档.md`
说明架构、边界与设计决策；实现、测试和构建配置是最终事实来源。

## 开始任务前

- 先执行 `git status --short`，保护已有的已跟踪和未跟踪修改。
- 阅读与任务相关的源码、测试、文档、CMake、脚本及部署配置，不只查看最近差异。
- 明确本次需求、文件范围和验收标准；优先交付可独立验证的最小完整切片。
- 以真实需求和现有架构为准，重复或边界稳定后再抽象，避免预先设计未被需要的扩展点。

## 平台、范围与依赖

- 项目仅支持 Linux 构建和运行，使用 C++20、CMake Presets、Ninja 和 vcpkg。
- 使用普通用户运行依赖准备、构建和测试。`scripts/install-deps.sh` 不会自行调用
  `sudo`；需要系统权限时应停止并让用户执行其输出的安装命令。
- 不提交 `build/`、`.tools/`、`.cache/`、`vcpkg_installed/`、日志、`.env` 或下载产物；
  文本文件保持 LF。
- 未经当前任务明确要求，不提前实现或宣称代理、缓存、限流、认证、TLS、HTTP/2
  等能力，也不无理由引入新生产依赖。

## 代码与架构

- 保持模块单一职责：`main` 负责命令行和装配，`config` 负责配置，`application`
  负责事件循环和生命周期，`http_server` 负责网络与会话，`http_handler` 负责响应映射，
  `logging` 负责结构化日志。不要把业务实现堆入 `main.cpp` 或 `application.cpp`。
- 遵循仓库的 `.clang-format`、`.clang-tidy` 和编译警告设置；优先使用 RAII 表达资源所有权。
- Asio/Beast 异步操作必须明确对象所有权、回调捕获、取消路径和关闭顺序，避免悬空引用、
  重复完成和关闭后的继续读写。修改单线程事件循环或引入并发前，先明确串行化与线程安全策略。
- 在创建监听器等外部副作用前严格校验配置；非法值应明确失败，错误信息不得泄露敏感值。
- 正常日志保持单行合法 JSON，并沿用结构化事件；不得记录认证头、Cookie、请求正文、
  完整查询参数、密码、令牌或其他凭据。
- 同步异常应在合适边界转换为清晰的非零退出或错误响应；异步错误必须检查并分类处理。
  不吞掉异常，不忽略 `error_code`，确保文件、socket、进程和其他资源在所有路径释放。

## 测试要求

- 纯配置和请求映射优先写单元测试；网络、进程生命周期、信号和真实协议行为写进程集成测试。
- 网络测试使用动态端口；所有等待都有明确超时，并通过事件、响应或进程状态同步，
  不用任意 `sleep` 代替可靠条件。
- 测试成功、失败和超时都必须清理其创建的进程、socket、临时文件、容器和镜像，
  且不得删除任务范围外的资源。
- 缺陷修复应增加能覆盖触发条件的回归测试。测试不得依赖固定机器路径、已有缓存或个人环境。

## 验证入口

按改动风险先运行针对性检查，再执行适当的完整入口：

```bash
bash scripts/format-check.sh
bash scripts/test.sh linux-debug
bash scripts/check.sh
bash scripts/test.sh linux-release
bash scripts/container-smoke.sh
```

- `scripts/check.sh` 是静态分析、警告即错误、ASan/UBSan 和测试的主要质量门禁。
- 涉及发布构建或安装行为时运行 Release 测试；涉及 Dockerfile、Compose 或运行镜像时，
  在 Docker 可用的环境执行容器烟雾测试。
- 不得通过降低检查标准、删除断言、跳过校验、吞掉失败或忽略退出码来获得绿色结果。
- 交付报告必须区分“实际执行并通过”“未执行”和“因权限、网络或工具缺失而受阻”；
  未实际运行的检查不得声称通过。

## Git、安全与文档

- 保留用户已有修改，不使用可能丢失内容的清理、重置或强制检出命令。
- 未经明确授权，不执行 `git add`、提交、推送、创建 PR、强制推送或其他远程状态修改。
- 不索取、生成、记录或输出密码、令牌和其他凭据；疑似秘密只报告位置和脱敏类型。
- 对外行为、命令或配置发生变化时同步更新相关测试及 `README.md`；架构和设计边界变化时
  更新 `设计文档.md`。明确区分当前实现与规划能力，不把测试覆盖等同于兼容承诺或生产就绪。
